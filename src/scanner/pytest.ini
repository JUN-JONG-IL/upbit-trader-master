[pytest]
# Run tests from this directory so pytest doesn't walk up to src/__init__.py
testpaths = scanner/tests
# Add scanner package to Python path
pythonpath = .
# Use importlib mode to avoid walking up to find parent packages
addopts = --import-mode=importlib
