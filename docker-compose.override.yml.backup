version: "3.8"
services:
  mlflow:
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; urllib.request.urlopen('http://127.0.0.1:5000', timeout=3)\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
